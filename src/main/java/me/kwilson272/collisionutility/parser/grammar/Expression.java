package me.kwilson272.collisionutility.parser.grammar;

/**
 * Interface representation of the expression grammar non-terminal.
 * This interface serves no real purpose other than to allow the two different
 * productions to be grouped together in a list.
 */
public interface Expression {
}
